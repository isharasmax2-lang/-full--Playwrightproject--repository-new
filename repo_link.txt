REPO_URL_PLACEHOLDER: (will be filled after pushing to GitHub)

To push this repository to GitHub yourself:

1. Create a new public repo at https://github.com/new (or use the `gh` CLI):

```powershell
gh repo create <your-username>/<repo-name> --public --source=. --remote=origin
```

2. Or manually:

```powershell
git remote add origin https://github.com/your-username/your-repo.git
git branch -M main
git push -u origin main
```

If you want me to push, provide a GitHub PAT with `repo` scope, or authorize `gh` in this environment.
